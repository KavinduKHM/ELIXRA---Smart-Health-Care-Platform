spring.application.name=doctor-service

# Use the same port everywhere (docker-compose, Eureka, and the embedded server)
server.port=8083

# Enable Eureka registration (docker-compose provides the Eureka URL)
eureka.client.enabled=true
